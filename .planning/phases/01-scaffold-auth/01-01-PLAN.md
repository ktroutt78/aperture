---
phase: 01-scaffold-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - .gitignore
  - .env.example
  - tsconfig.base.json
  - README.md
  - .nvmrc
  - extension/.gitkeep
  - backend/.gitkeep
  - demo-data/.gitkeep
  - docs/.gitkeep
autonomous: true
requirements:
  - SCAF-01
  - SCAF-04
user_setup: []

must_haves:
  truths:
    - "Root package.json declares a pnpm workspace with members extension, backend, demo-data, docs"
    - ".env.example declares every required env var from aperture-spec.md with empty values"
    - ".gitignore excludes .env files but allows .env.example"
    - "Empty aperture/ subdirectory is removed — monorepo lives at project root"
    - "Workspace directories (extension/, backend/, demo-data/, docs/) exist with .gitkeep placeholders"
  artifacts:
    - path: "package.json"
      provides: "Root workspace manifest with pnpm workspaces and Node 20 engine"
      contains: "\"packageManager\": \"pnpm"
    - path: "pnpm-workspace.yaml"
      provides: "pnpm workspace membership declaration"
      contains: "extension"
    - path: ".env.example"
      provides: "All 8 required env var keys with empty values"
      contains: "TABLEAU_PAT_SECRET="
    - path: ".gitignore"
      provides: "Excludes .env, node_modules, dist, build artifacts"
      contains: ".env"
    - path: "tsconfig.base.json"
      provides: "Shared strict TypeScript config inherited by both workspaces"
      contains: "\"strict\": true"
    - path: ".nvmrc"
      provides: "Locks Node version to 20 LTS"
      contains: "20"
  key_links:
    - from: "package.json"
      to: "pnpm-workspace.yaml"
      via: "pnpm workspace resolution"
      pattern: "packages:"
    - from: ".env.example"
      to: "backend consumers (Phase 1 plan 02, plan 04)"
      via: "shared env var keys"
      pattern: "TABLEAU_SERVER_URL"
---

<objective>
Scaffold the root of the Aperture monorepo: pnpm workspaces, shared TypeScript config, .env.example with every required variable, and empty workspace directories ready for the backend and extension.

Purpose: Phase 1 success criterion #1 requires a developer to clone, install, and run both apps from a single monorepo. This plan creates the foundation layer that plans 02, 03, and 04 build on top of.

Output: A working pnpm monorepo skeleton at /Users/keithtroutt/Documents/Projects/Aperture/ with four empty workspace directories, shared tsconfig, and a populated .env.example. No app code yet — just the scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/keithtroutt/Documents/Projects/Aperture/aperture-spec.md
@/Users/keithtroutt/Documents/Projects/Aperture/CLAUDE.md
@/Users/keithtroutt/Documents/Projects/Aperture/.planning/ROADMAP.md
@/Users/keithtroutt/Documents/Projects/Aperture/.planning/REQUIREMENTS.md
@/Users/keithtroutt/Documents/Projects/Aperture/.planning/STATE.md

<locked_decisions>
The following decisions are LOCKED by the planner (per CLAUDE.md: "Own all architecture decisions — do not ask for approval, make the call and log it"). Do NOT revisit.

- D-01: **Package manager** = **pnpm** (fastest install, first-class workspaces, best monorepo DX). Version: pnpm ≥ 9.0.
- D-02: **Workspace tool** = **pnpm workspaces** via `pnpm-workspace.yaml` (NOT npm workspaces, NOT yarn).
- D-03: **Node runtime** = **Node 20 LTS**, locked via `.nvmrc` and `engines.node` in package.json.
- D-04: **TypeScript config strategy** = one shared `tsconfig.base.json` at the root with `strict: true`, `target: ES2022`, `module: ESNext`, `moduleResolution: bundler`. Each workspace extends it and overrides `moduleResolution` as needed (backend → `node`, extension keeps `bundler`).
- D-05: **Env var loader (backend)** = `dotenv` (the native `--env-file` flag works but dotenv is explicit and well-understood). Extension uses Vite's native `import.meta.env.VITE_*`.
- D-06: **Monorepo root** = project root (`/Users/keithtroutt/Documents/Projects/Aperture/`). The empty `aperture/` subdirectory is removed in this plan.
</locked_decisions>

<env_vars_contract>
The 8 required env vars from `aperture-spec.md` (Environment Variables section). `.env.example` MUST declare exactly these keys with empty values:

```
TABLEAU_SERVER_URL=
TABLEAU_SITE_NAME=
TABLEAU_PAT_NAME=
TABLEAU_PAT_SECRET=
ANTHROPIC_API_KEY=
SLACK_WEBHOOK_URL=
PORT=3001
EXTENSION_ORIGIN=http://localhost:5173
```

Note: `PORT` and `EXTENSION_ORIGIN` get safe defaults because they are not secrets. All other values MUST be empty.
</env_vars_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize pnpm monorepo root with workspaces and shared tsconfig</name>
  <files>
    /Users/keithtroutt/Documents/Projects/Aperture/package.json
    /Users/keithtroutt/Documents/Projects/Aperture/pnpm-workspace.yaml
    /Users/keithtroutt/Documents/Projects/Aperture/tsconfig.base.json
    /Users/keithtroutt/Documents/Projects/Aperture/.nvmrc
    /Users/keithtroutt/Documents/Projects/Aperture/.gitignore
    /Users/keithtroutt/Documents/Projects/Aperture/README.md
    /Users/keithtroutt/Documents/Projects/Aperture/extension/.gitkeep
    /Users/keithtroutt/Documents/Projects/Aperture/backend/.gitkeep
    /Users/keithtroutt/Documents/Projects/Aperture/demo-data/.gitkeep
    /Users/keithtroutt/Documents/Projects/Aperture/docs/.gitkeep
  </files>
  <read_first>
    - /Users/keithtroutt/Documents/Projects/Aperture/aperture-spec.md (sections "Stack", "Repo Structure")
    - /Users/keithtroutt/Documents/Projects/Aperture/CLAUDE.md (Ground Rules + Key Technical Constraints)
    - /Users/keithtroutt/Documents/Projects/Aperture/.planning/ROADMAP.md (Phase 1 Success Criteria)
    - List the project root: `ls -la /Users/keithtroutt/Documents/Projects/Aperture/` — confirm there's an empty `aperture/` subdir to delete, and the files CLAUDE.md, LICENSE, aperture-spec.md already exist.
  </read_first>
  <action>
    Create the pnpm monorepo scaffold at `/Users/keithtroutt/Documents/Projects/Aperture/` (NOT inside the nested `aperture/` folder).

    **Step 1 — Remove empty nested directory:**
    ```bash
    rmdir /Users/keithtroutt/Documents/Projects/Aperture/aperture
    ```
    If the directory is not empty, list its contents first and ask user before deleting.

    **Step 2 — Write `/Users/keithtroutt/Documents/Projects/Aperture/package.json`** (per D-01, D-03):
    ```json
    {
      "name": "aperture",
      "version": "0.1.0",
      "private": true,
      "description": "Tableau Cloud Extension that embeds an AI analytics co-pilot",
      "packageManager": "pnpm@9.12.0",
      "engines": {
        "node": ">=20.0.0 <21.0.0"
      },
      "scripts": {
        "dev:backend": "pnpm --filter @aperture/backend dev",
        "dev:extension": "pnpm --filter @aperture/extension dev",
        "build": "pnpm -r build",
        "typecheck": "pnpm -r typecheck"
      },
      "devDependencies": {
        "typescript": "^5.5.4"
      }
    }
    ```

    **Step 3 — Write `/Users/keithtroutt/Documents/Projects/Aperture/pnpm-workspace.yaml`** (per D-02):
    ```yaml
    packages:
      - "extension"
      - "backend"
      - "demo-data"
      - "docs"
    ```

    **Step 4 — Write `/Users/keithtroutt/Documents/Projects/Aperture/tsconfig.base.json`** (per D-04):
    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "module": "ESNext",
        "moduleResolution": "bundler",
        "strict": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "forceConsistentCasingInFileNames": true,
        "resolveJsonModule": true,
        "isolatedModules": true,
        "noUncheckedIndexedAccess": true,
        "noImplicitOverride": true,
        "useDefineForClassFields": true,
        "allowSyntheticDefaultImports": true
      }
    }
    ```

    **Step 5 — Write `/Users/keithtroutt/Documents/Projects/Aperture/.nvmrc`** (per D-03):
    ```
    20
    ```

    **Step 6 — Write `/Users/keithtroutt/Documents/Projects/Aperture/.gitignore`:**
    ```
    # Dependencies
    node_modules/
    .pnpm-store/

    # Build outputs
    dist/
    build/
    *.tsbuildinfo

    # Env files (secrets)
    .env
    .env.local
    .env.*.local
    !.env.example

    # IDE
    .vscode/*
    !.vscode/extensions.json
    !.vscode/settings.json
    .idea/

    # OS
    .DS_Store
    Thumbs.db

    # Logs
    logs/
    *.log
    npm-debug.log*
    pnpm-debug.log*

    # Coverage
    coverage/
    .nyc_output/
    ```

    **Step 7 — Write `/Users/keithtroutt/Documents/Projects/Aperture/README.md`** (stub, Phase 5 fills it out):
    ```markdown
    # Aperture

    Tableau Cloud Extension that embeds an AI analytics co-pilot inside any published dashboard. Fuses Tableau Metadata API, VizQL Data Service, and Pulse REST API and streams merged context through Claude for narrative intelligence, anomaly detection, and guided follow-up questions.

    See `aperture-spec.md` for the full technical spec.

    ## Quick start

    ```bash
    nvm use                 # Node 20
    pnpm install
    cp .env.example .env    # fill in TABLEAU_* and ANTHROPIC_API_KEY
    pnpm dev:backend        # starts backend on http://localhost:3001
    pnpm dev:extension      # starts extension on http://localhost:5173
    ```

    ## Structure

    - `extension/` — Tableau Extension (Vite + React + TypeScript)
    - `backend/` — TypeScript backend API (Fastify)
    - `demo-data/` — Sample data setup instructions
    - `docs/` — SETUP.md, JUDGING.md, ARCHITECTURE.md

    Full setup, architecture, and judging docs land in Phase 5.
    ```

    **Step 8 — Create workspace directories with .gitkeep placeholders** so pnpm recognizes them and git tracks them:
    ```bash
    mkdir -p /Users/keithtroutt/Documents/Projects/Aperture/extension
    mkdir -p /Users/keithtroutt/Documents/Projects/Aperture/backend
    mkdir -p /Users/keithtroutt/Documents/Projects/Aperture/demo-data
    mkdir -p /Users/keithtroutt/Documents/Projects/Aperture/docs
    touch /Users/keithtroutt/Documents/Projects/Aperture/extension/.gitkeep
    touch /Users/keithtroutt/Documents/Projects/Aperture/backend/.gitkeep
    touch /Users/keithtroutt/Documents/Projects/Aperture/demo-data/.gitkeep
    touch /Users/keithtroutt/Documents/Projects/Aperture/docs/.gitkeep
    ```

    **Step 9 — Run `pnpm install`** at the root to materialize the lockfile. This will fail gracefully if no workspace package.json files exist yet (expected — plans 02 and 03 add them). If it fails, that's fine — the workspace manifest is still valid and later plans will complete install.
  </action>
  <verify>
    <automated>
      cd /Users/keithtroutt/Documents/Projects/Aperture && \
      test -f package.json && \
      test -f pnpm-workspace.yaml && \
      test -f tsconfig.base.json && \
      test -f .nvmrc && \
      test -f .gitignore && \
      test -f README.md && \
      test -d extension && \
      test -d backend && \
      test -d demo-data && \
      test -d docs && \
      ! test -d aperture && \
      grep -q '"packageManager": "pnpm' package.json && \
      grep -q '"strict": true' tsconfig.base.json && \
      grep -q 'extension' pnpm-workspace.yaml && \
      grep -q '.env' .gitignore && \
      grep -q '!.env.example' .gitignore && \
      echo "monorepo-foundation-OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `/Users/keithtroutt/Documents/Projects/Aperture/package.json` exists and contains `"packageManager": "pnpm@9.12.0"` and `"node": ">=20.0.0`
    - `/Users/keithtroutt/Documents/Projects/Aperture/pnpm-workspace.yaml` lists `extension`, `backend`, `demo-data`, `docs` as packages
    - `/Users/keithtroutt/Documents/Projects/Aperture/tsconfig.base.json` contains `"strict": true` and `"target": "ES2022"`
    - `/Users/keithtroutt/Documents/Projects/Aperture/.nvmrc` contains `20`
    - `/Users/keithtroutt/Documents/Projects/Aperture/.gitignore` contains both `.env` (excluded) and `!.env.example` (allowed)
    - Directories `extension/`, `backend/`, `demo-data/`, `docs/` all exist at the project root
    - The empty `aperture/` subdirectory is removed (does not exist)
    - `pnpm install` runs without a fatal config error (may warn about missing workspace packages — that's OK at this stage)
  </acceptance_criteria>
  <done>
    Monorepo foundation files exist at project root, pnpm workspace declaration is valid, shared tsconfig.base.json with strict mode is in place, `.gitignore` excludes `.env` but allows `.env.example`, and all four workspace directories exist. Empty nested `aperture/` directory is deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example with all 8 required variables</name>
  <files>
    /Users/keithtroutt/Documents/Projects/Aperture/.env.example
  </files>
  <read_first>
    - /Users/keithtroutt/Documents/Projects/Aperture/aperture-spec.md (section "Environment Variables" — authoritative list)
    - /Users/keithtroutt/Documents/Projects/Aperture/.gitignore (written in Task 1, confirms .env.example is NOT gitignored)
    - If `.env.example` already exists at the target path, read it first before overwriting.
  </read_first>
  <action>
    Write `/Users/keithtroutt/Documents/Projects/Aperture/.env.example` with EXACTLY these 8 keys, values empty for secrets and safe defaults for non-secrets (per SCAF-04 and the env_vars_contract in context):

    ```
    # Tableau Cloud connection — get these from Tableau Cloud > Account Settings > Personal Access Tokens
    # Server URL is your Tableau Cloud pod, e.g. https://10ay.online.tableau.com
    TABLEAU_SERVER_URL=
    # Site content URL (the URL-safe site name, NOT the display name)
    TABLEAU_SITE_NAME=
    # Personal Access Token name (displayed when you create the PAT)
    TABLEAU_PAT_NAME=
    # Personal Access Token secret (only shown once at creation — regenerate if lost)
    TABLEAU_PAT_SECRET=

    # Anthropic API key for Claude (claude-sonnet-4-20250514)
    ANTHROPIC_API_KEY=

    # Slack incoming webhook URL for "Push to Slack" feature (Phase 3)
    SLACK_WEBHOOK_URL=

    # Backend server port (default: 3001)
    PORT=3001

    # Origin where the extension dev server runs (used for CORS allowlist)
    EXTENSION_ORIGIN=http://localhost:5173
    ```

    **CRITICAL — security:**
    - No real values. ALL secret keys (TABLEAU_PAT_SECRET, TABLEAU_PAT_NAME, TABLEAU_SERVER_URL, TABLEAU_SITE_NAME, ANTHROPIC_API_KEY, SLACK_WEBHOOK_URL) must be empty after the `=` sign.
    - Only `PORT=3001` and `EXTENSION_ORIGIN=http://localhost:5173` have values (non-secret defaults).
    - Comments describe what each var is for and where to get it.
    - This file IS committed to git (per .gitignore rule `!.env.example`).
    - Mitigates threat T-01 (secret exfiltration) and T-02 (credential on disk).
  </action>
  <verify>
    <automated>
      cd /Users/keithtroutt/Documents/Projects/Aperture && \
      test -f .env.example && \
      grep -q '^TABLEAU_SERVER_URL=$' .env.example && \
      grep -q '^TABLEAU_SITE_NAME=$' .env.example && \
      grep -q '^TABLEAU_PAT_NAME=$' .env.example && \
      grep -q '^TABLEAU_PAT_SECRET=$' .env.example && \
      grep -q '^ANTHROPIC_API_KEY=$' .env.example && \
      grep -q '^SLACK_WEBHOOK_URL=$' .env.example && \
      grep -q '^PORT=3001$' .env.example && \
      grep -q '^EXTENSION_ORIGIN=http://localhost:5173$' .env.example && \
      ! test -f .env && \
      echo "env-example-OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `/Users/keithtroutt/Documents/Projects/Aperture/.env.example` exists
    - File contains all 8 required keys (grep matches for each)
    - All 6 secret keys have empty values (lines match `^KEY=$`)
    - `PORT=3001` and `EXTENSION_ORIGIN=http://localhost:5173` have the documented default values
    - No real `.env` file is created (only `.env.example`)
    - File is tracked by git (run `git check-ignore .env.example` — should return non-zero meaning NOT ignored)
  </acceptance_criteria>
  <done>
    `.env.example` exists at project root with all 8 env vars from the spec, no real secrets, and is tracked by git (not ignored). Developers can `cp .env.example .env` and fill in their own credentials.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git repo → public | Files committed here may become public if repo is published. No secrets allowed. |
| filesystem → developer shell | `.env` may exist locally; must never be committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env.example` | mitigate | All secret keys have empty values; comments describe location but never include real tokens |
| T-01-02 | Information Disclosure | git repo | mitigate | `.gitignore` excludes `.env`, `.env.local`, `.env.*.local`; explicitly allows `.env.example` via `!.env.example` |
| T-01-06 | Denial of Service | `/health` endpoint | accept | `/health` has no input, negligible attack surface, documented |

</threat_model>

<verification>
After both tasks complete, verify the monorepo foundation is valid:

```bash
cd /Users/keithtroutt/Documents/Projects/Aperture
# Structure checks
test -f package.json && test -f pnpm-workspace.yaml && test -f tsconfig.base.json
test -f .nvmrc && test -f .gitignore && test -f .env.example && test -f README.md
test -d extension && test -d backend && test -d demo-data && test -d docs
! test -d aperture  # nested empty dir removed

# Content checks
grep -q '"packageManager": "pnpm' package.json
grep -q 'packages:' pnpm-workspace.yaml
grep -q '"strict": true' tsconfig.base.json
grep -q '^TABLEAU_PAT_SECRET=$' .env.example   # empty secret
grep -q '^PORT=3001$' .env.example             # non-empty default
grep -q '!.env.example' .gitignore             # explicit allowlist

# Security check — no real secrets leaked
! grep -E '(pat_[A-Za-z0-9]{20,}|sk-[A-Za-z0-9]{40,}|xoxb-)' .env.example
```

All commands must exit 0 (the last one uses `!` so success means no matches found).
</verification>

<success_criteria>
- Plan 01 delivers a valid pnpm workspace root that plans 02 (backend) and 03 (extension) can add packages to
- `.env.example` fully specifies the env contract that plan 02 (backend dotenv load) and plan 04 (Tableau auth) will consume
- No secrets committed; `.gitignore` correctly excludes `.env` but allows `.env.example`
- Phase 1 Success Criterion #1 partially satisfied (monorepo with workspaces) and Criterion #5 fully satisfied (`.env.example` declares every required var)
- SCAF-01 and SCAF-04 are fully addressed
</success_criteria>

<output>
After completion, create `/Users/keithtroutt/Documents/Projects/Aperture/.planning/phases/01-scaffold-auth/01-01-SUMMARY.md` documenting:
- Files created with paths
- pnpm version pinned
- Workspace members declared
- Env vars declared in .env.example (just the keys, never values)
- Confirmation that `.env` is gitignored and `.env.example` is not
- Any deviations from locked decisions (there should be none)
</output>
